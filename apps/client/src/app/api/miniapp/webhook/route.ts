export async function GET() {
  return new Response('ok', { status: 200 });
}

export async function POST(_request: Request) {
  // Accept and ignore payload for now; can be wired later
  return new Response('ok', { status: 200 });
}










